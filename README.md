
  # GLAZEE

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.



  Datos extra sobre la implementacion de Strip:
  necesitaras implementar las llaves publicas y secretas en .env y checkout 
